/**
 * Local storage keys
 * All data stored to local storage is prefixed with `com.zazuko.sketch`.
 * 
 * @module constants/local-storage-keys
 * @preferred
 */
export const localStoragePrefix = 'com.zazuko.sketch';
export const localStorageKeyText = `${localStoragePrefix}.text`;
export const localStorageKeyFormat = `${localStoragePrefix}.format`;
export const localStorageKeyPrefixList = `${localStoragePrefix}.prefix`;